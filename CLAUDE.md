# CLAUDE.md

このファイルは、このリポジトリで作業するClaude Codeへの指示です。

## シークレット(APIキー等)の扱いについて — 厳守事項

- **`.env` の中身を読まない・catしない・grepしない・要約もしない。** 内容を確認する必要がある
  ように思えても、`OPENAI_API_KEY` が設定されているかどうかは
  `[ -n "${OPENAI_API_KEY:-}" ]` のような存在チェック、または
  `scripts/set_openai_key.sh` の成功/失敗の出力で判断すること。値そのものを見に行かない。
- **ユーザーにAPIキー・トークン・パスワードなどのシークレットをチャットに貼らせない。**
  貼ろうとしていたら止めて、代わりに `scripts/set_openai_key.sh`
  (クリップボード経由で `.env` に保存するスクリプト)の利用を案内すること。
- もし何らかの理由でシークレットがチャット/コミット/ログに既に貼られてしまった場合、
  それは漏洩済みとして扱う。そのシークレットを使い続けたり保存したりせず、
  ユーザーに直ちに失効・再発行するよう伝えること。
- `.env` は `.gitignore` 済み。新しい環境変数ファイルを追加する場合も同様にコミット対象から
  除外すること。
- シークレットを含む可能性のあるファイルをコミット・プッシュする前は、必ず内容を確認すること
  (これは通常のセキュリティ配慮であり、上の「.envを読まない」ルールの例外ではなく、
  `.env` 自体をコミット対象に含めようとした場合はまずその意図を疑うべき、という意味)。
